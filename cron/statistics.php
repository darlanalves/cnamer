<?php

// load the redirect log

// count up the redirects

// count the unique cache files

// work out how many new redirects and domains since the last run

// update the stats file to incl. total redirects + domains and period redirects + domains

// archive the log

// generate a new index and push it out

// log the result of this update or something